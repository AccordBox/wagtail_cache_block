============
Installation
============

At the command line::

    $ easy_install wagtail_cache_block

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv wagtail_cache_block
    $ pip install wagtail_cache_block
